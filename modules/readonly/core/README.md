# Core

Readonly trusted module.

Owns the Codex SDK bridge, persistent Codex thread state, and the single `askAgent()` function used by ingress modules.

Files:

- `index.ts`: Codex SDK thread/streaming, prompt formatting, and `.axiom/core.json`
- `paths.ts`: project paths
